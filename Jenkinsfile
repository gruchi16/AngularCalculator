pipeline {
   agent {
  any
    }
     
    
    stages{
        stage('build Analysis') {
           steps {
               sh 'npm install && ng build'
              
             }
          }
       
         
    }
}
